<Window x:Class="CleanMyCodexApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Clean My Codex"
        Width="1280"
        Height="820"
        MinWidth="900"
        MinHeight="620"
        WindowStartupLocation="CenterScreen"
        Background="#050505">
  <Grid Background="#050505">
    <wv2:WebView2 x:Name="Browser" Visibility="Collapsed" />
    <Grid x:Name="StartupPanel" Background="#050505">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid Width="30" Height="30" HorizontalAlignment="Center">
          <Ellipse Stroke="#303030" StrokeThickness="2" />
          <Ellipse Stroke="#F4F4F4" StrokeThickness="2"
                   StrokeDashArray="1 3" RenderTransformOrigin="0.5,0.5">
            <Ellipse.RenderTransform>
              <RotateTransform x:Name="LoadingRotation" />
            </Ellipse.RenderTransform>
            <Ellipse.Triggers>
              <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                  <Storyboard RepeatBehavior="Forever">
                    <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                     Storyboard.TargetProperty="Angle"
                                     From="0" To="360" Duration="0:0:1" />
                  </Storyboard>
                </BeginStoryboard>
              </EventTrigger>
            </Ellipse.Triggers>
          </Ellipse>
        </Grid>
        <TextBlock x:Name="StartupLabel"
                   Margin="0,14,0,0"
                   Foreground="#A1A1A1"
                   FontFamily="Segoe UI"
                   FontSize="12"
                   Text="Starting Clean My Codex..." />
      </StackPanel>
    </Grid>
  </Grid>
</Window>
